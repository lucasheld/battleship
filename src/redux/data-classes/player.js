export default class Player {
    id;
    nick;
    pin;
    avatar;
    constructor(id, nick, pin, avatar) {
        this.id = id;
        this.nick = nick;
        this.pin = pin;
        this.avatar = avatar;
    }
}
