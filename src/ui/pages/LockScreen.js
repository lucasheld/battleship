import React, { Component } from "react";

export default class LockScreen extends Component {
    render() {
        return (
            <div>
                LockScreen
            </div>
        );
    }
}
